[project]
name = "mist-core"
version = "0.1.0"
description = "Shared infrastructure for MIST (storage, settings, Ollama client)"
requires-python = ">=3.13"
dependencies = ["ollama"]

[project.optional-dependencies]
dev = ["pytest", "pytest-asyncio"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
