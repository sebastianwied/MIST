[project]
name = "mist-server"
version = "0.1.0"
description = "FastAPI server for the MIST desktop app"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "uvicorn",
    "mist-agent",
]

[tool.setuptools]
py-modules = []
